{
  "name": "campground-signup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "prisma generate && next build",
    "start": "prisma db push --accept-data-loss && next start -H 0.0.0.0 -p ${PORT:-8080}",
    "dev": "next dev",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "next": "^14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "prisma": "^5.22.0",
    "typescript": "^5.5.4"
  }
}
